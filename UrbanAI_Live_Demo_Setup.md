# Urban AI - Live Demo & Mock Data Purge Plan
**Context for AI Agent:** We are finalizing the Urban AI MVP (FastAPI + React). The codebase is fully implemented. The user explicitly requested to **remove ALL mock data** from the frontend and backend. The system must operate strictly on live data generated by real WhatsApp messages via Twilio and processed by the FastAPI backend.

Please execute the following phases step-by-step and confirm upon completion of each phase.

## Phase 1: Purge Mock Data from Frontend (React)
- [x] 1. Inspect `ClientsPage.jsx`, `DashboardPage.jsx`, and any other dashboard components.
- [x] 2. Identify and completely remove any static arrays (e.g., `const MOCK_LEADS = [...]` or `const dummyData`).
- [x] 3. Ensure the state (e.g., `const [leads, setLeads] = useState([])`) is strictly populated by the `axios.get('http://localhost:8000/api/leads')` response.
- [x] 4. Ensure the `DashboardPage.jsx` KPI metrics (Revenue, Open Tasks, etc.) are populated purely by the `/api/stats` endpoint. If the endpoint does not support all metrics yet, set the unsupported ones to `0` instead of using fake numbers.
- [x] 5. Verify the `setInterval` (polling) logic is correctly fetching fresh data every 3 seconds.

## Phase 2: Database Reset (Clean Slate)
- [x] 1. Provide the user with a Python script (or terminal command) to delete the existing `urban_ai.db` file and recreate it empty. This ensures the user starts testing with zero records.
- [x] 2. Ensure `models.Base.metadata.create_all(bind=engine)` is correctly set in `main.py` to auto-recreate the tables when the server restarts.

## Phase 3: Ngrok & Twilio Webhook Configuration
- [x] 1. Instruct the user to run the FastAPI server: `uvicorn main:app --reload`.
- [x] 2. Instruct the user to run the React frontend: `npm run dev`.
- [x] 3. Instruct the user to open a new terminal and run: `ngrok http 8000`.
- [x] 4. Print explicit instructions for the user on how to copy the generated Ngrok `Forwarding HTTPS URL` and append `/webhook/whatsapp` to it.
- [x] 5. Provide step-by-step instructions on where to paste this exact URL in the Twilio Console (Sandbox settings -> "When a message comes in").

## Phase 4: Live Testing Protocol (Terminal Output)
- [x] 1. Output a "Live Testing Guide" for the user to execute from their own WhatsApp.
- [x] 2. Include these exact 3 test cases for the user to send to the Twilio Sandbox number:
    - **Test 1 (Hot Lead):** "مساء الخير، عايز أعرف تفاصيل الأسعار وهل متاح حجز ميتنج قريب؟"
    - **Test 2 (Qualified):** "إيه هي طبيعة الخدمات اللي بتقدموها في الشركة؟"
    - **Test 3 (Escalation):** "أنا محتاج أتواصل مع الإدارة أو حد بشري عشان عايز خصم خاص للمشروع بتاعي."
- [x] 3. Instruct the user to keep the React Dashboard open on one half of their screen and their WhatsApp on the other half to witness the real-time live polling in action without page refreshes.
